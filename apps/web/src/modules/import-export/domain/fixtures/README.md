# Renewlet Import Fixtures

`v0.3.22-renewlet-export-v1.json` and its ZIP representation are fixed compatibility samples for tag `v0.3.22`, commit `e12a95e6fd9a24fd40fb842dc23ded338f4ef7b7`.

The ZIP contains the exact JSON sample as `data.json` plus a v1 `manifest.json`. Keep both files immutable; compatibility tests must not regenerate an old backup from the current schema.
